# 🚀 GitHub Pages 部署指南

## 步骤1：创建GitHub仓库

1. 访问 https://github.com
2. 点击右上角 "+" → "New repository"
3. 填写仓库名称（例如：`student-report-app`）
4. 选择 Public（GitHub Pages需要公开仓库）
5. **不要**勾选 "Initialize this repository with a README"
6. 点击 "Create repository"

## 步骤2：初始化Git仓库并推送代码

在项目目录下执行以下命令：

```bash
# 初始化Git仓库
git init

# 添加所有文件
git add .

# 提交代码
git commit -m "Initial commit: 学生报告展示应用"

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/仓库名.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

## 步骤3：启用GitHub Pages

1. 在GitHub仓库页面，点击 "Settings"
2. 在左侧菜单找到 "Pages"
3. 在 "Source" 下选择：
   - Source: **GitHub Actions**
4. 保存设置

## 步骤4：等待自动部署

1. 点击仓库顶部的 "Actions" 标签
2. 等待 "Deploy to GitHub Pages" 工作流完成（约2-3分钟）
3. 部署完成后，在 "Settings" → "Pages" 中可以看到你的网站地址

## 步骤5：获取部署链接

部署完成后，你的网站地址格式为：
```
https://你的用户名.github.io/仓库名/
```

例如：
```
https://username.github.io/student-report-app/
```

## 步骤6：使用部署后的链接

1. 打开"接收数据"对话框
2. 在"方案2：使用部署域名"中输入你的GitHub Pages域名
   - 例如：`username.github.io/student-report-app`
3. 粘贴JSON数据并生成链接
4. 生成的链接格式：`https://username.github.io/student-report-app/?data=...`
5. 手机可以直接打开这个链接！

## 🔄 更新部署

每次修改代码后：

```bash
git add .
git commit -m "更新说明"
git push
```

GitHub Actions会自动重新构建和部署，约2-3分钟后更新完成。

## ⚙️ 配置说明

- `.github/workflows/deploy.yml` - GitHub Actions自动部署配置
- `vite.config.ts` - 已配置构建输出目录为 `build`
- `package.json` - 已配置构建脚本

## 📱 优势

- ✅ 完全免费
- ✅ 自动HTTPS
- ✅ 全球CDN加速
- ✅ 手机可以直接访问
- ✅ 链接永久有效
- ✅ 自动部署（推送代码后自动更新）

## ⚠️ 注意事项

1. **仓库必须是公开的**（GitHub Pages免费版要求）
2. **首次部署需要等待几分钟**
3. **每次更新代码后需要等待部署完成**
4. **域名格式**：`用户名.github.io/仓库名`

## 🐛 常见问题

### Q: 部署失败怎么办？
A: 
- 检查 "Actions" 标签中的错误信息
- 确保 `package.json` 中有 `build` 脚本
- 确保所有依赖都已安装

### Q: 如何自定义域名？
A: 
- 在仓库 Settings → Pages → Custom domain 中配置
- 需要配置DNS记录

### Q: 部署后404错误？
A: 
- 确保 `vite.config.ts` 中 `base` 设置为 `/仓库名/`
- 或者使用根域名部署（需要特殊配置）

