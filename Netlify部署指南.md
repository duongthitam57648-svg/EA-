# Netlify部署指南

## 🚀 快速部署到Netlify

Netlify也是免费的静态网站托管服务。

### 步骤1：构建项目

```bash
npm run build
```

### 步骤2：部署

**方式A：使用Netlify网站**

1. 访问 https://www.netlify.com
2. 注册/登录账号
3. 点击 "Add new site" → "Deploy manually"
4. 将 `build` 文件夹拖拽到页面
5. 等待部署完成

**方式B：使用Netlify CLI**

```bash
npm install -g netlify-cli
netlify deploy --prod --dir=build
```

### 步骤3：配置SPA路由

创建 `netlify.toml` 文件（已创建），Netlify会自动处理路由。

### 步骤4：获取部署链接

部署完成后，你会得到一个链接，例如：
```
https://your-project-name.netlify.app
```

## 📱 优势

- ✅ 免费
- ✅ 全球CDN
- ✅ HTTPS自动配置
- ✅ 手机可以直接访问

