# GitHub推送完整步骤

## 🚀 最简单的方法：使用批处理脚本

我已经创建了 `推送到GitHub.bat` 文件，请：

1. **双击运行** `推送到GitHub.bat`
2. 按照提示操作
3. 如果提示输入GitHub仓库地址，先完成下面的步骤1

## 📋 完整步骤说明

### 步骤1：创建GitHub仓库（必须先完成）

1. 访问 https://github.com 并登录
2. 点击右上角 **"+"** → **"New repository"**
3. 填写信息：
   - Repository name: `student-report-app`（或你喜欢的名字）
   - Description: `学生报告展示应用`
   - **重要**：选择 **Public** ✅（GitHub Pages需要公开仓库）
   - **不要**勾选 "Initialize this repository with a README"
4. 点击 **"Create repository"**
5. **复制仓库地址**（页面会显示，例如：`https://github.com/你的用户名/student-report-app.git`）

### 步骤2：推送代码

**方法A：使用批处理脚本（推荐）**

1. 双击运行 `推送到GitHub.bat`
2. 当提示输入GitHub仓库地址时，粘贴步骤1中复制的地址
3. 按回车继续

**方法B：手动执行命令**

在项目文件夹中，右键点击空白处 → "在终端中打开" 或 "Open in Terminal"，然后执行：

```bash
git init
git add .
git commit -m "Initial commit: 学生报告展示应用"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

**如果提示需要认证：**

1. 用户名：输入你的GitHub用户名
2. 密码：**不要输入密码**，使用Personal Access Token

**如何获取Token：**
- GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
- 点击 "Generate new token (classic)"
- Note: 填写 `Git Push`
- 勾选 `repo` 权限
- 点击 "Generate token"
- **复制Token**（只显示一次，请保存好）
- 在提示输入密码时，粘贴这个Token

### 步骤3：启用GitHub Pages

1. 在GitHub仓库页面，点击 **"Settings"**（设置）
2. 在左侧菜单找到 **"Pages"**
3. 在 "Source" 下选择：**GitHub Actions** ✅
4. 点击 **"Save"**（保存）

### 步骤4：等待自动部署

1. 点击仓库顶部的 **"Actions"** 标签
2. 你会看到 "Deploy to GitHub Pages" 工作流正在运行
3. 等待约 **2-3分钟**，直到看到绿色的 ✅ 标记
4. 部署完成后，在 **Settings → Pages** 中可以看到你的网站地址

### 步骤5：获取部署链接

部署完成后，你的网站地址格式为：
```
https://你的用户名.github.io/仓库名/
```

例如：
```
https://username.github.io/student-report-app/
```

### 步骤6：使用部署后的链接

1. 打开应用，点击"接收数据"按钮
2. 在"方案2：使用部署域名"中输入你的GitHub Pages域名
   - 例如：`username.github.io/student-report-app`
3. 粘贴JSON数据并生成链接
4. 生成的链接格式：`https://username.github.io/student-report-app/?data=...`
5. **手机可以直接打开这个链接！** 📱

## ✅ 完成！

现在你的应用已经部署到GitHub Pages，分享链接可以在任何设备上打开了！

## 🔄 后续更新

每次修改代码后，执行：
```bash
git add .
git commit -m "更新说明"
git push
```

GitHub会自动重新部署。

