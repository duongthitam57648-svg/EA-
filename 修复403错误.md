# 🔧 修复403权限错误

## 错误分析

看到错误：
- `Permission denied to GHVBUGB`
- `403 Forbidden`

**原因**：
1. Git当前使用的用户是 `GHVBUGB`，但仓库属于 `duongthitam57648-svg`
2. 认证失败或使用了错误的凭据
3. 需要清除旧的Git凭据并使用正确的Token

## 快速修复

**双击运行** `修复权限并推送.bat`，脚本会自动：
1. 清除旧的Git凭据
2. 配置正确的Git用户信息
3. 重新配置远程仓库
4. 推送到GitHub

## 手动修复步骤

### 步骤1：清除Git凭据

```bash
# 清除Windows凭据管理器中的Git凭据
git credential-manager-core erase
# 或者手动清除：
# 控制面板 → 凭据管理器 → Windows凭据 → 删除所有github.com相关凭据
```

### 步骤2：配置正确的Git用户

```bash
git config --global user.name "duongthitam57648-svg"
git config --global user.email "你的邮箱@example.com"
```

### 步骤3：重新配置远程仓库

```bash
git remote remove origin
git remote add origin https://github.com/duongthitam57648-svg/EA-.git
```

### 步骤4：获取Personal Access Token

1. 访问 https://github.com/settings/tokens
2. Generate new token (classic)
3. Note: `Git Push`
4. **重要**：勾选 `repo` 权限
5. 生成并复制Token

### 步骤5：推送（使用Token）

```bash
git push -u origin main
```

当提示输入密码时：
- 用户名：`duongthitam57648-svg`
- 密码：**粘贴Token**（不是GitHub密码）

## 如果还是失败

### 检查仓库权限

1. 访问 https://github.com/duongthitam57648-svg/EA-
2. 点击 Settings → Collaborators
3. 确认你的账号有写入权限

### 使用SSH方式（备选）

如果HTTPS一直失败，可以尝试SSH：

```bash
# 生成SSH密钥
ssh-keygen -t ed25519 -C "你的邮箱@example.com"

# 复制公钥
cat ~/.ssh/id_ed25519.pub

# 添加到GitHub: Settings → SSH and GPG keys → New SSH key

# 使用SSH地址
git remote set-url origin git@github.com:duongthitam57648-svg/EA-.git
git push -u origin main
```

## 推荐操作

1. **先运行** `修复权限并推送.bat`
2. 按照提示获取Token
3. 推送时使用Token作为密码

如果还有问题，告诉我具体的错误信息！

