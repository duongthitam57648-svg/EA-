# 🔧 修复远程仓库错误并推送

## 问题

看到错误：`error: remote origin already exists`

这表示远程仓库已经配置过了，需要先删除再重新添加。

## 快速修复

**双击运行** `修复并推送.bat`，脚本会自动：
1. 删除旧的远程仓库配置
2. 重新添加正确的远程仓库
3. 检查并提交所有文件
4. 推送到GitHub

## 手动修复命令

如果脚本不工作，在PowerShell中执行：

```bash
# 1. 删除旧的远程仓库
git remote remove origin

# 2. 重新添加远程仓库
git remote add origin https://github.com/duongthitam57648-svg/EA-.git

# 3. 检查状态
git status

# 4. 如果有未提交的文件，添加并提交
git add .
git commit -m "Update: 更新项目文件"

# 5. 推送（如果提示认证，使用Token）
git push -u origin main

# 如果推送失败，尝试强制推送
git push -u origin main --force
```

## 推送成功后

1. 访问 https://github.com/duongthitam57648-svg/EA-
2. 点击 Settings → Pages
3. Source 选择：GitHub Actions
4. 等待部署完成
5. 访问：https://duongthitam57648-svg.github.io/EA-/

