# 🚀 完整项目推送指南

## 快速推送（推荐）

**双击运行** `完整推送项目.bat`，脚本会自动完成所有步骤。

## 手动推送步骤

如果脚本不工作，在项目文件夹中打开PowerShell或CMD，按顺序执行：

### 步骤1：初始化Git仓库

```bash
git init
```

### 步骤2：配置Git用户信息（如果还没配置）

```bash
git config --global user.name "duongthitam57648-svg"
git config --global user.email "你的邮箱@example.com"
```

### 步骤3：添加所有文件

```bash
git add .
```

### 步骤4：提交代码

```bash
git commit -m "Initial commit: 学生报告展示应用 - 完整项目"
```

### 步骤5：配置远程仓库

```bash
git remote remove origin 2>nul
git remote add origin https://github.com/duongthitam57648-svg/EA-.git
```

### 步骤6：设置主分支

```bash
git branch -M main
```

### 步骤7：推送到GitHub

```bash
git push -u origin main
```

**如果提示需要认证**：
- 用户名：`duongthitam57648-svg`
- 密码：使用Personal Access Token（不是GitHub密码）

**如果推送失败，尝试强制推送**：
```bash
git push -u origin main --force
```

## 🔐 获取Personal Access Token

1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. Note: 填写 `Git Push`
4. **重要**：勾选 `repo` 权限
5. 点击 "Generate token"
6. **立即复制Token**（只显示一次！）
7. 推送时作为密码使用

## ✅ 推送成功后

1. 访问 https://github.com/duongthitam57648-svg/EA-
2. 点击 **Settings** → **Pages**
3. Source 选择：**GitHub Actions**
4. 等待2-3分钟自动部署完成
5. 访问：https://duongthitam57648-svg.github.io/EA-/

## 📱 使用部署链接

部署完成后，在应用中：
1. 点击"接收数据"按钮
2. 输入域名：`duongthitam57648-svg.github.io/EA-`
3. 生成分享链接
4. 手机可以直接打开！

